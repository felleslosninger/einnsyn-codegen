// Auto-generated from our OpenAPI spec
// https://github.com/felleslosninger/ein-openapi/

import ApiClientBase from './ApiClientBase';

{{#each entityList}}
  {{#unless schema.x-isExtended}}
    import { {{capitalize name}}Resource } from './resource/{{capitalize name
    }}Resource';
  {{/unless}}
{{/each}}

export default class ApiClient extends ApiClientBase {
{{#each entityList}}
  {{! Extended schemas are abstract, and cannot be instantiated  }}
  {{#unless schema.x-isExtended}}
    public {{deCapitalize name}} = new {{capitalize name}}Resource(this);
  {{/unless}}
{{/each}}
}