// Auto-generated from our OpenAPI spec
// https://github.com/felleslosninger/ein-openapi/

package {{java-client-package-name}}.entities.{{lc entityName}};

import java.lang.reflect.Type;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;
import com.google.gson.JsonPrimitive;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;

{{#each resources}}
  {{#if (ne . ../entityName)}}
    import {{java-client-package-name}}.entities.{{lc .}}.{{capitalize .}};
  {{/if}}
{{/each}}

public class UnionResource{{capitalize propertyName}}TypeAdapter {

  class Serializer implements JsonSerializer<UnionResource{{capitalize propertyName}}> {
    public JsonElement serialize(UnionResource{{capitalize propertyName}} src, Type typeOfSrc, JsonSerializationContext context) {
      {{#each resources}}
      if (src.get{{capitalize .}}() != null) {
        return context.serialize(src.get{{capitalize .}}(), {{capitalize .}}.class);
      }
      {{/each}}
      return new JsonPrimitive(src.getId());
    }
  }


  class Deserializer implements JsonDeserializer<UnionResource{{capitalize propertyName}}> {
    public UnionResource{{capitalize propertyName}} deserialize(JsonElement json, Type typeOfT, JsonDeserializationContext context) throws JsonParseException {
      if (json.isJsonNull()) {
        return null;
      }

      if (json.isJsonPrimitive()) {
        JsonPrimitive jsonPrimitive = json.getAsJsonPrimitive();
        if (jsonPrimitive.isString()) {
          return new UnionResource{{capitalize propertyName}}(jsonPrimitive.getAsString());
        }
      }

      if (json.isJsonObject()) {
        JsonObject jsonObject = json.getAsJsonObject();
        String entity = jsonObject.get("entity").getAsString();
        switch (entity) {
          {{#each resources}}
            case "{{capitalize .}}":
              return context.deserialize(json, {{capitalize .}}.class);
          {{/each}}
            default:
        }
      }

      throw new JsonParseException("Could not deserialize UnionResourceParent");
    }
  }
}