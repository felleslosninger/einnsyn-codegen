// Auto-generated from our OpenAPI spec
// https://github.com/felleslosninger/ein-openapi/

package {{java-client-package-name}}.entities.{{lc entityName}};

{{#each resources}}
  {{#if (ne x-resourceId ../entityName)}}
    import {{java-client-package-name}}.entities.{{lc x-resourceId}}.{{capitalize x-resourceId}};
  {{/if}}
{{/each}}
import {{java-client-package-name}}.common.hasid.HasId;

public class {{className}} implements HasId {

  private String id;
  public String getId() {
    return id;
  }

  {{#each resources}}
    private {{capitalize x-resourceId}} {{deCapitalize x-resourceId}};
    public {{capitalize x-resourceId}} get{{capitalize x-resourceId}}() {
      return {{deCapitalize x-resourceId}};
    }
  {{/each}}

  public {{className}}(String id) {
    this.id = id;
  }

  {{#each resources}}
    public {{../className}}({{capitalize x-resourceId}} {{deCapitalize x-resourceId}}) {
      this.{{deCapitalize x-resourceId}} = {{deCapitalize x-resourceId}};
      this.id = {{deCapitalize x-resourceId}}.getId();
    }
  {{/each}}

  public boolean isExpanded() {
    return
      {{#each resources}}{{deCapitalize x-resourceId}} != null{{#unless @last}} || {{/unless}}{{/each}};
  }
}
