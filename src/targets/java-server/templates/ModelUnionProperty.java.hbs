// Auto-generated from our OpenAPI spec
// https://github.com/felleslosninger/ein-openapi/

package {{java-package-name}}.entities.{{lc entityName}}.models;

import jakarta.validation.Valid;
import lombok.Getter;
import lombok.Setter;

{{#each resources}}
  {{#if (ne . ../entityName)}}
    import {{java-package-name}}.entities.{{lc .}}.models.{{capitalize .}}DTO;
  {{/if}}
{{/each}}
import {{java-package-name}}.entities.HasId;

@Getter
@Setter
public class UnionProperty{{capitalize propertyName}} implements HasId {

  String id;

  {{#each resources}}
    @Valid
    {{capitalize .}}DTO {{deCapitalize .}};
  {{/each}}

  public UnionProperty{{capitalize propertyName}}(String id) {
    this.id = id;
  }
}
