// Auto-generated from our OpenAPI spec
// https://github.com/felleslosninger/ein-openapi/

package {{java-package-name}}.entities.{{lc entityName}}.models;

import jakarta.validation.Valid;
import lombok.Getter;
import lombok.Setter;

{{#each resources}}
  {{#if (ne . ../entityName)}}
    import {{java-package-name}}.entities.{{lc .}}.models.{{capitalize .}}DTO;
  {{/if}}
{{/each}}
import {{java-package-name}}.common.hasid.HasId;

@Getter
@Setter
public class UnionResource{{capitalize propertyName}} implements HasId {

  String id;

  {{#each resources}}
    @Valid
    {{capitalize .}}DTO {{deCapitalize .}};
  {{/each}}

  public UnionResource{{capitalize propertyName}}(String id) {
    this.id = id;
  }

  {{#each resources}}
    public UnionResource{{capitalize ../propertyName}}({{capitalize .}}DTO {{deCapitalize .}}) {
      this.{{deCapitalize .}} = {{deCapitalize .}};
      this.id = {{deCapitalize .}}.getId();
    }
  {{/each}}
}
